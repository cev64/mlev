<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android's own splash screen: it shows the icon on the app's background
         while the process starts, and hands over as soon as Compose is ready.
         Deliberately no artificial delay. -->
    <style name="Theme.Mlev.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Mlev</item>
    </style>

    <style name="Theme.Mlev" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
