<?xml version="1.0" encoding="utf-8"?>
<!--
  The app talks to a computer on your own Wi-Fi over plain HTTP, so cleartext
  has to be permitted. It is scoped to private address ranges and .local names
  rather than opened globally, so the app cannot be pointed at an unencrypted
  site on the public internet.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">local</domain>
    </domain-config>
</network-security-config>
